solve UI updates before conversion ends issue
show only one notification after ENTIRE compression is done

show the conversion done from the BACK! the front only shows data coming from the back and does NOT count the files left on itself - u could use Claude for that.

solve this white window thing
ADD TESTING!